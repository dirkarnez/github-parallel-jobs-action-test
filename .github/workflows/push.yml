name: parallel-jobs-actions-workflow
on: push
jobs:
  example-job-a:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Retrieve IP
        run: |
          ifconfig -a
  example-job-b:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Retrieve IP
        run: |
          ifconfig -a
